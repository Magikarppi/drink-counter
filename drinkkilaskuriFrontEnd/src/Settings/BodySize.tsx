import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const styles = StyleSheet.create({});

const BodySize = () => {
  return (
  <View style={styles}>
    <Text>link to update body size</Text>
  </View>
  )
};

export default BodySize;